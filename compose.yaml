services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: azure-vision-webapp
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      FLASK_HOST: 0.0.0.0
      FLASK_PORT: 5000
      FLASK_DEBUG: "false"
    restart: unless-stopped
